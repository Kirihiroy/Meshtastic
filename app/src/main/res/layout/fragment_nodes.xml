<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingTop="16dp">

    <!-- Заголовок: лого + "Ноды" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:contentDescription="@string/cd_logo"
            android:src="@drawable/ic_logo_mesh"
            app:tint="@color/app_on_surface" />

        <TextView
            android:id="@+id/nodes_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/nodes_title"
            android:textColor="@color/app_on_surface"
            android:textSize="28sp"
            android:textStyle="bold" />
    </LinearLayout>

    <TextView
        android:id="@+id/nodes_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="44dp"
        android:layout_marginTop="2dp"
        android:textColor="@color/app_on_surface_muted"
        android:textSize="14sp"
        tools:text="(онлайн 1 / показано 3 / всего 3)" />

    <!-- Статус-баннер (бывшая Панель состояния — теперь компактно вверху Нод) -->
    <LinearLayout
        android:id="@+id/nodes_status_banner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:background="@drawable/bg_settings_card"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <View
                android:id="@+id/nodes_status_dot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:background="@drawable/bg_node_badge" />

            <TextView
                android:id="@+id/nodes_status_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/app_on_surface"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="Подключено" />

            <TextView
                android:id="@+id/nodes_status_last_rx"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/app_on_surface_muted"
                android:textSize="12sp"
                tools:text="RX 2 мин назад" />
        </LinearLayout>

        <TextView
            android:id="@+id/nodes_status_metrics"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/app_on_surface_muted"
            android:textSize="12sp"
            tools:text="Батарея 85% · SNR 8.5 · firm 2.5.4" />
    </LinearLayout>

    <!-- Поле фильтра + кнопка сортировки -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:background="@drawable/bg_filter_field"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="14dp">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:contentDescription="@string/cd_search"
                android:src="@drawable/ic_search"
                app:tint="@color/app_on_surface_muted" />

            <EditText
                android:id="@+id/nodes_filter"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginStart="10dp"
                android:autofillHints="no"
                android:background="@android:color/transparent"
                android:hint="@string/nodes_filter_hint"
                android:imeOptions="actionSearch"
                android:importantForAutofill="no"
                android:inputType="text"
                android:maxLines="1"
                android:textColor="@color/app_on_surface"
                android:textColorHint="@color/app_on_surface_muted"
                android:textSize="15sp" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/nodes_sort"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/bg_sort_button"
            android:contentDescription="@string/cd_sort"
            android:src="@drawable/ic_sort"
            app:tint="@color/app_on_surface" />
    </LinearLayout>

    <TextView
        android:id="@+id/empty_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="@string/nodes_empty"
        android:textColor="@color/app_on_surface_muted"
        android:textSize="14sp"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/nodes_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="12dp" />

</LinearLayout>
