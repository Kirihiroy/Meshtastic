<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/node_filter_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/app_outline" />
    <corners android:radius="10dp" />
</shape>
